# Password Checker

A minimal, secure password-checker project (API + frontend) that evaluates password strength and returns helpful feedback.

## Features
- Password strength scoring (length, character classes, patterns)
- Client-side strength meter (fast UX)
- Server endpoint to validate rules/score
- Unit tests and CI

## Quickstart
See `backend/README.md` and `frontend/index.html`.
